// Type declarations for Volto 18+
// In Volto 16, the config is imported from '@plone/volto/registry' and doesn't use module augmentation
// This file is kept for reference when migrating back to Volto 18+

/*
import '@plone/registry';
import type { RichResultTypeConfig } from './registry';

declare module '@plone/registry' {
  export interface ConfigType {
    settings: {
      richresults?: {
        types: Record<string, RichResultTypeConfig>;
      };
      [key: string]: any;
    };
  }
}
*/
